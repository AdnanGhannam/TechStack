import { RequestHandler } from "express";
import IToolkitsController from "../interfaces/IToolkitsController";

const createEndpoint: RequestHandler = () => {

};

const getAllEndpoint: RequestHandler = () => {
    
};

const getByIdEndpoint: RequestHandler = () => {

};
    
const updateEndpoint: RequestHandler = () => {
    
};
    
const removeEndpoint: RequestHandler = () => {
    
};

const controller: IToolkitsController = {
    createEndpoint,
    getAllEndpoint,
    getByIdEndpoint,
    updateEndpoint,
    removeEndpoint,
};

export default controller;